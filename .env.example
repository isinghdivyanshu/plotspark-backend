# ---------------------------------
# REQUIRED - MUST BE FILLED IN
# ---------------------------------

# Database Connection for Local IDE Development
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/plotspark
SPRING_DATASOURCE_USERNAME=admin
SPRING_DATASOURCE_PASSWORD=your_secret_password

# JWT Secrets (generate long, random strings for these)
JWT_ACCESS_SECRET=your_super_secret_for_access_tokens
JWT_REFRESH_SECRET=your_other_super_secret_for_refresh_tokens

# Mail Server Credentials
SPRING_MAIL_HOST=smtp.example.com
SPRING_MAIL_USERNAME=your_email
SPRING_MAIL_PASSWORD=your_email_password

# -----------------------------------------------------------------
# OPTIONAL - The application has sensible defaults for these.
# Only add them to your .env file if you need to override them.
# -----------------------------------------------------------------

# The port the backend server will run on (Default: 8080)
# SERVER_PORT=8080

# The active development profile (Default: dev)
# SPRING_PROFILES_ACTIVE=dev

# The port for the mail server (Default: 587)
# SPRING_MAIL_PORT=587

# The base URL for the frontend, used for generating email links (Default: http://localhost:3000)
# FRONTEND_BASE_URL=http://localhost:3000

# JWT Expiration Times in Milliseconds
# Default Access Token Expiration: 900000 (15 minutes)
# JWT_ACCESS_TOKEN_EXPIRATION_MS=900000

# Default Refresh Token Expiration: 604800000 (7 days)
# JWT_REFRESH_TOKEN_EXPIRATION_MS=604800000